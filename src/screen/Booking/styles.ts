import { StyleSheet } from 'react-native';
import { colors } from '../../theme/colors';

const styles = StyleSheet.create({
    noteAddModalContainer: {
        backgroundColor: colors.WHITE_COLOR,
        padding: 10,
        borderRadius: 6,
        marginHorizontal: 15
    },
});
export default styles;
